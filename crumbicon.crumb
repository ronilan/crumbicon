debug = { thing ->
  (print "\e[24;1H")
  (print thing)
  (print "         ")
}

file = (if (is (length arguments) 2) {<- (get arguments 1)} {<- "favicon.svg"})

data = (use "./usables/tui.crumb" "components.crumb" {
  <- (run components state)
})

(use "export.crumb" {
  (svg data (list 16 16 10) file)
})


